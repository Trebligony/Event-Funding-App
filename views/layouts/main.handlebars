<!DOCTYPE html>
<html lang="en">

<head>
    <title>Crowdfunding</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <script src="/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark static-top ">
            <div class="container">
                <a class="navbar-brand" href="/"> <img src="/image/logo.png" alt="Crowdfunding" height="80">
                </a>
                <h3>Fund My Project</h3>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav ms-auto">
                        {{#if logged_in}}
                        <li class="nav-item">
                            <a class="nav-link active" href="/">HOME</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/profile">PROFILE</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" type="button" href="" id="logout">| &nbsp; LOGOUT</a>
                        </li>
                        {{else}}
                        <li class="nav-item ">
                            <a class="nav-link active" href="/">HOME</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" type="button" href="/login">| &nbsp; LOGIN</a>
                        </li>
                        {{/if}}
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <!-- HEADER END -->
    <!-- MAIN START -->
    <main>
        {{{ body }}}
    </main>
    <!-- MAIN END -->

    <!-- FOOTER START -->
    <footer class="mt-5 p-4 bg-dark text-white text-center position-sticky">
        <div>
            <p>Crowdfunding &copy; All Rights Reserved.</p>
        </div>
    </footer>
    <!-- FOOTER END -->

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}

</body>

</html>